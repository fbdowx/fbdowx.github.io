$(document).ready((function(){const i=$(".accordion");i.find(".accordion-item:first-child").addClass("is-active").find(".accordion-panel").slideDown(),i.on("click",".accordion-item",(function(c){const n=$(this),o=n.hasClass("is-active");i.find(".accordion-item.is-active").not(n).each((function(){const i=$(this);i.removeClass("is-active"),i.find(".accordion-panel").slideUp()})),o?(n.removeClass("is-active"),n.find(".accordion-panel").slideUp()):(n.addClass("is-active"),n.find(".accordion-panel").slideDown())}))}));
const scriptUrls = [
  '/path/to/your/script1.js',
  '/path/to/your/script2.js'
  // Add more script URLs as needed
];

function loadScripts() {
  const version = Date.now(); // Generate a unique version identifier (timestamp)
  
  scriptUrls.forEach(url => {
    const script = document.createElement('script');
    script.src = `${url}?v=${version}`; // Append version as query parameter
    script.async = false; // Load scripts synchronously for simplicity
    
    document.head.appendChild(script);
  });
}

// Call loadScripts() to load updated versions of scripts
loadScripts();
